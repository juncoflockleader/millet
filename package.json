{
  "name": "millet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Generalized multiplayer turn-based card game engine.",
  "scripts": {
    "demo": "node --experimental-strip-types scripts/serve-demo.mjs",
    "test": "node scripts/run-tests.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
